"""
recommendations/ — not yet implemented.

Populated in a later milestone per Section 12 of the project spec
(docs/PROJECT_SPEC.md). M1 only builds window/ + ui/ (skeleton) —
this package intentionally stays empty until its milestone comes up,
so we don't get ahead of the spec's build order.
"""
